services:
  rift:
    image: rift-proxy:local
    ports:
      - "2525:2525"   # Admin API
      - "4547:4547"   # Fault Injection demo
    volumes:
      - ./imposters-rift-features.json:/imposters.json:ro
    environment:
      - RUST_LOG=info
      - MB_PORT=2525
    command: ["--configfile", "/imposters.json"]
    healthcheck:
      test: ["CMD", "rift", "healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 5
